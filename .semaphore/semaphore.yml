version: v1.0
name: Demo
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
execution_time_limit:
  minutes: 2
blocks:
  - name: Setup
    dependencies: []
    task:
      jobs:
        - name: bundle
          commands:
            - echo "bundle"
  - name: Build
    dependencies:
      - Setup
    task:
      jobs:
        - name: Beleza
          commands:
            - echo "build beleza"
        - name: Boticario
          commands:
            - echo "build boticário"
after_pipeline:
  task:
    jobs:
      - name: Publish Results
        commands:
          - test-results gen-pipeline-report
